#!/bin/bash

PEFILE=PKGROOT/rocks/threaded.pe
PENAME=threaded
QUEUE=all.q

if [ ! -f $PEFILE ]; then
    echo  "Parallel environment file  $PEFILE is not found" 
    exit 1
fi

qconf -Ap $PEFILE
qconf -aattr queue pe_list $PENAME $QUEUE  
