<?xml version="1.0" standalone="no"?>

<kickstart>


	<description>
	The Atlas Roll: opal server install
	</description>

	<copyright>
	Copyright (c) 2000 - 2012 The Regents of the University of California.
	All rights reserved. Rocks(r) v5.5/v6.0 www.rocksclusters.org
	</copyright>

	<changelog>
	</changelog>

	<package>atlas-opal</package>

<post>
if test -d /opt/opal; then

<file name="/opt/opal/deployed/deploy.sh" mode="append">
# FTMap 
atlasver=ATLASVER
atlas_bin=/opt/atlas-ATLASVER/bin/run_atlas
if test -e $atlas_bin; then
    $ANT -f $OPALXML deploy -DserviceName=FTMap_${atlasver} -DappConfig=$OPALCONFIG/FTMap_${atlasver}.xml
fi

# flex 
flexver=ATLASVER
flexbin=/opt/atlas-ATLASVER/bin/flex_atlas
if test -e $flexbin; then
    $ANT -f $OPALXML deploy -DserviceName=FTFlex_${flexver} -DappConfig=$OPALCONFIG/FTFlex_${flexver}.xml
fi

</file>

fi

</post>

</kickstart>
